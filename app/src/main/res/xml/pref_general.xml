
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <SwitchPreference
        android:defaultValue="true"
        android:key="example_switch"
        android:summary="Enables Sorority events to appear on list/swipe mode"
        android:title="Search for Sorority Events" />

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <SwitchPreference
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:defaultValue="true"
        android:key="switch_preference_1"
        android:summary="Enables Fraternity events to appear on list/swipe mode"
        android:title="Search for Fraternity Events" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <Preference android:title="Logout"
        android:key="logout"
        android:summary="Log you out of Chad Meets Stacey. We'll miss you!"/>

    <Preference android:title="Delete Account"
        android:key="deleteAccount"
        android:summary="Deletes your account. There is no going back"/>
</PreferenceScreen>


